<h1><%= @user.username %></h1>
<p><%= @user.email %></p>
<p><%= t("register_at") %> <%= @user.created_at %></p>


<main class="container">
  <div class="articles__container">
    <%= render @posts %>
  </div>
  <%= will_paginate @posts %>
</main>